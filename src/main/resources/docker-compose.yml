version: '3.8'
services:
  redis:
    image: "redis:alpine"
    container_name: redis_container
    ports:
      - "6379:6379"
    networks:
      - redis-network
networks:
  redis-network:
    driver: bridge
